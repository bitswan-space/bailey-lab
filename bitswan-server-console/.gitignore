node_modules/
dist/
*.log

# Heavy design captures — the JSX/HTML/CSS reference under wireframe/ is kept
# (small, useful for backend wiring), but the screenshot/upload PNGs (~5MB) are
# not committed.
wireframe/screenshots/
wireframe/uploads/
